# profiles-template.yml
# Copy this file to your local dbt profiles directory and rename it to profiles.yml.
# Then replace the placeholders below with your own values.
dbt_demo:
  outputs:
    dev:
      dataset: dbt_demo_dev              # e.g. feel free to use your own (existing) dataset
      job_execution_timeout_seconds: 300
      job_retries: 1
      keyfile: <FULL_PATH_TO_YOUR_JSON_KEY> # e.g. C:\Users\beto_\Downloads\compsci-x-407-9-87abce5b61f7.json
      location: US
      method: service-account
      priority: interactive
      project: compsci-x-407-9
      threads: 1
      type: bigquery
  target: dev
